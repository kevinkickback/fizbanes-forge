# Fizbane's Forge Environment Variables
# 
# Copy this file to .env and adjust values as needed.
# All processes (main, renderer, and tests) use this configuration.

# Enable debug mode (verbose logging, no console suppression, cache bypass)
# Also exposes debug utilities in DevTools console via window.__debug
# Set to 'true' to enable, 'false' or omit to disable
FF_DEBUG=false

# Enable DevTools on startup (separate from debug mode)
# Set to 'true' to enable, 'false' or omit to disable
FF_DEVTOOLS=false

# Use bundled data from /src/data (bypasses configured sources)
# Primarily for development and testing with local data files
# Set to 'true' to enable, 'false' or omit to disable
FF_USE_BUNDLED_DATA=false
